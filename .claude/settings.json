{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; echo \"$f\" | grep -qE '\\.rs$' && cargo fmt --all --manifest-path /home/giuda14/helios/Cargo.toml; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Formatting Rust..."
          }
        ]
      }
    ]
  }
}
